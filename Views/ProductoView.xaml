<UserControl x:Class="Sachiel.Views.ProductoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sachiel"
        mc:Ignorable="d">
    
    <Grid Margin="15,15,15,15">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Buscador -->
        <GroupBox Style="{StaticResource group}" Grid.Row="0" Grid.Column="0" Margin="0,0,0,10">
            <GroupBox.Header>
                <TextBlock Text="Buscar producto existente" Style="{StaticResource title}"/>
            </GroupBox.Header>
            <Grid Margin="10">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" VerticalContentAlignment="Center" Padding="0,0,5,0" Content="Nombre o ID:"/>
                <TextBox Grid.Column="1" x:Name="txtBuscar" VerticalContentAlignment="Center" Margin="10,0"/>

                <StackPanel Grid.Column="2" MinHeight="30" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource btn}" x:Name="btnBuscar" Content="Buscar" Click="btnBuscar_Click"/>
                </StackPanel>
                

            </Grid>
        </GroupBox>
        
        <!-- Nuevo producto -->
        <GroupBox Style="{StaticResource group}" Grid.Row="0" Grid.Column="1" Margin="0,0,10,10">
            <GroupBox.Header>
                <TextBlock x:Name="titulo" Text="Añadir nuevo producto" Style="{StaticResource title}"/>
            </GroupBox.Header>
            <Grid Margin="10">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" VerticalContentAlignment="Center" Padding="0,0,5,0" Content="Nombre:"/>
                <TextBox Grid.Column="1" x:Name="txtNombre" VerticalContentAlignment="Center"/>

                <Label Grid.Column="2" VerticalAlignment="Center" Content="Precio:" Padding="0,0,5,0" Margin="15,0,0,0"/>
                <TextBox Grid.Column="3" x:Name="txtPrecio" VerticalContentAlignment="Center"/>

                <StackPanel Grid.Column="4" MinHeight="30" Orientation="Horizontal" HorizontalAlignment="Right">

                    <Button x:Name="btnAgregar" Style="{StaticResource btn}" Margin="0,0,10,0" Content="Añadir" Click="btnAgregar_Click"/>
                    <Button x:Name="btnLimpiar" Style="{StaticResource btn}" Content="Limpiar" Click="btnLimpiar_Click"/>

                </StackPanel>

            </Grid>
        </GroupBox>


        <!-- Tabla -->
        <DataGrid Grid.Row="1" ColumnHeaderStyle="{StaticResource HeaderDataGrid}" Grid.Column="0" Grid.ColumnSpan="2" x:Name="dgProductos" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem x:Name="MenuModificar" Header="Modificar" Click="MenuModificar_Click"/>
                    <MenuItem x:Name="MenuEliminar" Header="Eliminar" Click="MenuEliminar_Click"/>
                </ContextMenu>                
            </DataGrid.ContextMenu>

            <DataGrid.Columns>

                <DataGridTextColumn Header="Id" Binding="{Binding Id, StringFormat=X3}" Width="70" IsReadOnly="True"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding Precio}" Width="150"/>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
