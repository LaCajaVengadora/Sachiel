<Window x:Class="Sachiel.Views.ProductoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sachiel"
        mc:Ignorable="d"
        Title="Productos" Height="450" Width="800">
    
    <Grid Margin="15,15,15,15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!-- Nuevo producto -->
        <GroupBox Header="Añadir nuevo producto" Grid.Row="0" Margin="0,0,0,10">
            <Grid Margin="10">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>

                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Content="Nombre:"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="txtNombre"/>
                
                <Label Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Content="Precio:" Margin="15,0,0,0"/>
                <TextBox Grid.Row="0" Grid.Column="3" x:Name="txtPrecio"/>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="6" Orientation="Horizontal" Height="30" Margin="0,10,0,0">

                    <Button x:Name="btnAgregar" Width="100" Margin="0,0,10,0" Content="Añadir"/>
                    <Button x:Name="btnLimpiar" Width="120" Content="Limpiar campos"/>

                </StackPanel>

            </Grid>
        </GroupBox>

        <!-- Buscador -->
        <GroupBox Header="Productos existentes" Grid.Row="1" Margin="0,0,0,10">
            <Grid Margin="10" Height="25">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0" VerticalAlignment="Center" Content="Producto:"/>
                <TextBox Grid.Column="1" Margin="10,0" x:Name="txtBuscar"/>
                <Button Grid.Column="2" Width="75" x:Name="btnBuscar" Content="Buscar"/>

            </Grid>
        </GroupBox>

        <!-- Tabla -->
        <DataGrid Grid.Row="2" x:Name="dgProductos" AutoGenerateColumns="False" IsReadOnly="False" CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>

                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="70" IsReadOnly="True"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding Precio}" Width="150"/>

            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</Window>
